<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <link rel="icon" href="/images/logo.png" />
  <title>Accueil - Librairie de Mohamed</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body {
      background: linear-gradient(135deg, #f8fafc 0%, #e0e7ff 100%);
      min-height: 100vh;
      font-family: 'Segoe UI', 'Roboto', Arial, sans-serif;
    }
    .hero-section {
      background: linear-gradient(120deg, #6366f1 0%, #60a5fa 100%);
      color: #fff;
      border-radius: 1.5rem;
      box-shadow: 0 4px 32px rgba(99,102,241,0.15);
      padding: 3rem 2rem 2rem 2rem;
      margin-bottom: 2.5rem;
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    .hero-section::after {
      content: '';
      position: absolute;
      top: -60px;
      right: -60px;
      width: 180px;
      height: 180px;
      background: rgba(255,255,255,0.08);
      border-radius: 50%;
      z-index: 0;
    }
    .hero-section h1 {
      font-size: 2.8rem;
      font-weight: 700;
      letter-spacing: -1px;
      margin-bottom: 1rem;
      z-index: 1;
      position: relative;
    }
    .hero-section p {
      font-size: 1.25rem;
      margin-bottom: 1.5rem;
      z-index: 1;
      position: relative;
    }
    .feature-card {
      border: none;
      border-radius: 1rem;
      box-shadow: 0 2px 16px rgba(96,165,250,0.08);
      transition: transform 0.2s;
      background: #fff;
    }
    .feature-card:hover {
      transform: translateY(-6px) scale(1.03);
      box-shadow: 0 6px 32px rgba(99,102,241,0.13);
    }
    .feature-icon {
      font-size: 2.5rem;
      color: #6366f1;
      margin-bottom: 0.5rem;
    }
    .admin-info {
      background: linear-gradient(90deg, #f472b6 0%, #facc15 100%);
      color: #fff;
      border-radius: 0.75rem;
      padding: 1rem 1.5rem;
      margin-bottom: 1.5rem;
      font-size: 1.05rem;
      font-weight: 500;
      box-shadow: 0 2px 12px rgba(250,204,21,0.08);
    }
    @media (max-width: 767px) {
      .hero-section {
        padding: 2rem 1rem 1.5rem 1rem;
      }
      .hero-section h1 {
        font-size: 2rem;
      }
    }
  </style>
</head>
<body>
  <%- include('partials/navbar', { user }) %>
  <div class="container py-4">
    <section class="hero-section mb-5">
      <h1>Bienvenue à la Librairie en ligne</h1>
      <p>Découvrez une vaste sélection de livres pour tous les goûts, dans une expérience moderne et intuitive.</p>
      <div class="d-flex flex-wrap justify-content-center gap-3">
        <a href="/books" class="btn btn-lg btn-warning fw-bold shadow-sm px-4 py-2">
          <i class="bi bi-book-half me-2"></i>Voir les livres
        </a>
        <% if (user && user.role === 1) { %>
          <a href="/admin/users?userId=<%= user._id %>" class="btn btn-lg btn-success fw-bold shadow-sm px-4 py-2">
            <i class="bi bi-speedometer2 me-2"></i>Tableau de bord Admin
          </a>
        <% } %>
      </div>
    </section>

    <div class="row g-4 mb-5">
      <div class="col-md-4">
        <div class="card feature-card h-100 text-center p-4">
          <div class="feature-icon mb-2"><i class="bi bi-search"></i></div>
          <h5 class="card-title fw-bold">Recherche Facile</h5>
          <p class="card-text">Trouvez rapidement vos livres préférés grâce à notre moteur de recherche performant.</p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card feature-card h-100 text-center p-4">
          <div class="feature-icon mb-2"><i class="bi bi-bag-check"></i></div>
          <h5 class="card-title fw-bold">Achat Sécurisé</h5>
          <p class="card-text">Profitez d'une expérience d'achat fluide et sécurisée, adaptée à tous les appareils.</p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card feature-card h-100 text-center p-4">
          <div class="feature-icon mb-2"><i class="bi bi-people"></i></div>
          <h5 class="card-title fw-bold">Communauté Active</h5>
          <p class="card-text">Rejoignez une communauté de lecteurs passionnés et partagez vos avis sur les livres.</p>
        </div>
      </div>
    </div>

    <div class="admin-info">
      <i class="bi bi-info-circle-fill me-2"></i>
      Pour accéder au tableau de bord admin, utilisez : <strong>Email:</strong> AdminAdmin@gmail.com, <strong>Mot de passe:</strong> Admin1
    </div>
  </div>
  <div class="container">
    <%- include('partials/footer') %>
  </div>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <script src="/js/client.js"></script>
</body>
</html>