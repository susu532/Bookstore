<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Connexion - Librairie de Mohamed</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body {
      min-height: 100vh;
      background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
      display: flex;
      flex-direction: column;
    }
    .login-card {
      background: #fff;
      border-radius: 1.5rem;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.2);
      padding: 2.5rem 2rem;
      max-width: 400px;
      margin: 3rem auto;
      transition: box-shadow 0.3s;
    }
    .login-card:hover {
      box-shadow: 0 12px 40px 0 rgba(31, 38, 135, 0.3);
    }
    .form-control:focus {
      border-color: #2575fc;
      box-shadow: 0 0 0 0.2rem rgba(37,117,252,.25);
    }
    .btn-primary {
      background: linear-gradient(90deg, #6a11cb 0%, #2575fc 100%);
      border: none;
      transition: background 0.3s;
    }
    .btn-primary:hover {
      background: linear-gradient(90deg, #2575fc 0%, #6a11cb 100%);
    }
    .brand-logo {
      width: 60px;
      height: 60px;
      object-fit: contain;
      margin-bottom: 1rem;
      border-radius: 50%;
      box-shadow: 0 2px 12px rgba(106,17,203,0.15);
    }
    .form-label {
      color: #2575fc;
      font-weight: 500;
    }
    .register-link {
      color: #6a11cb;
      font-weight: 500;
      text-decoration: none;
    }
    .register-link:hover {
      text-decoration: underline;
      color: #2575fc;
    }
    @media (max-width: 576px) {
      .login-card {
        padding: 2rem 1rem;
        margin: 2rem 0.5rem;
      }
    }
  </style>
</head>
<body>
  <%- include('partials/navbar') %>
  <div class="container d-flex flex-column justify-content-center align-items-center" style="flex:1;">
    <div class="login-card shadow-lg">
      <div class="text-center mb-4">
        <img src="/images/logo.png" alt="Librairie Logo" class="brand-logo">
        <h2 class="fw-bold" style="color:#6a11cb;">Bienvenue !</h2>
        <p class="text-muted mb-0">Connectez-vous à votre compte</p>
      </div>
      <form id="loginForm" autocomplete="on">
        <div class="mb-3">
          <label for="email" class="form-label">Email</label>
          <input type="email" class="form-control" id="email" placeholder="Entrez votre email" value="AdminAdmin@gmail.com" required>
        </div>
        <div class="mb-3">
          <label for="password" class="form-label">Mot de passe</label>
          <input type="password" class="form-control" id="password" placeholder="Entrez votre mot de passe" value="Admin" required>
        </div>
        <div class="d-grid mb-2">
          <button type="submit" class="btn btn-primary btn-lg">Se connecter</button>
        </div>
      </form>
      <div class="text-center mt-3">
        <span class="text-muted">Pas de compte ?</span>
        <a href="/register" class="register-link">Inscrivez-vous</a>
      </div>
    </div>
  </div>
  <div class="container">
    <%- include('partials/footer') %>
  </div>
  <script src="/js/client.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>